---
description: Build and publish standalone exe
---

Run the publish process:
1. First run `dotnet build` to check for errors
2. If the build succeeds, run `.\publish.ps1` to create the standalone exe
3. Report the output location and any issues
