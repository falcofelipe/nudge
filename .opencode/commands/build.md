---
description: Build the project and fix any errors
---

Run `dotnet build` from the repo root. If there are any build errors or warnings, fix them all. After fixing, re-run the build to confirm it succeeds cleanly.
